# Decision log

Every decision taken on this project and the reasoning behind it.

**Relationship to ADRs.** An ADR is for a decision that is expensive to reverse — one that would
cost a migration, a rewrite spanning more than one module, or a change to a published contract.
This log is for everything else: the smaller calls that shape the code and would otherwise survive
only in a chat transcript. Where a decision has an ADR, the log row points at it rather than
repeating it.

**Graduation rule.** If a row's *Why* cannot be said in one sentence, it is probably an ADR.
Write the ADR and reduce the row to a pointer.

**Status.** `Active` · `Superseded by Dnnn` · `Reversed` · `Probation` (will be deleted unless a
condition is met by a stated date)

IDs are chronological, so reading top to bottom shows how the design evolved. Grouping is by area,
so reading a section shows what governs one part of the system.

---

## Process and scope

| ID | Decision | Why | Status |
|---|---|---|---|
| D001 | Send ten clarifying questions before writing any code | The brief was open to interpretation and the assessment explicitly rewards clarifying. Three answers reversed decisions already made — asking cost one document, building on the inference would have cost two days | Active |
| D002 | Build to the customer's confirmed scope, not the inferred one | The role is titled Software Craftsperson; overriding written scope guidance demonstrates the opposite. [ADR-0012](adr/0012-scope-confirmed-with-customer.md) | Active |
| D003 | Keep the superseded temporal design in the repo rather than deleting it | A reversal made on evidence is more informative to a reviewer than a tidy history | Active |
| D004 | Angular 19 rather than React, despite the brief listing React first | The JD is Java/Angular; confirmed acceptable by the customer | Active |
| D005 | Three-day build, submit Saturday 12 Sep | Scope shrank ~40% after clarification, and the customer said early submission is prioritised | Active |
| D006 | Deploy to Render + Neon free tier | Public URL was the requirement; cost and setup time were not worth spending on AWS here | Active |
| D007 | Scale headroom is documented, not built | 1M employees is ~3M rows and ~2.5 GB — small for PostgreSQL. Building for it would contradict "not the most complex system". [06-SCALABILITY.md](06-SCALABILITY.md) gives eight steps with measurable triggers | Active |
| D008 | Reply to the customer confirming the six scope changes in writing | Removes ambiguity about what was built, and timestamps the start | Active |

## Product scope

| ID | Decision | Why | Status |
|---|---|---|---|
| D009 | Payroll processing out of scope | A separate regulated product per country; the persona's problem is understanding pay, not paying | Active |
| D010 | Dashboard with KPI cards and filters is the primary deliverable | Confirmed by the customer as the target; it is also what a spreadsheet cannot do | Active |
| D011 | Annual base salary only, in local currency | Confirmed. Model shaped so components can be added as typed siblings | Active |
| D012 | Salary bands displayed, never enforced | Confirmed — enforcement and alerting are future scope | Active |
| D013 | What-if simulation, review tracking and point-in-time snapshots dropped | Confirmed deferrable; each depends on history the customer said was unnecessary | Active |
| D014 | Approval workflow deferred, but the actor is recorded on every change | A workflow engine is orthogonal to the compensation domain. [ADR-0009](adr/0009-defer-approval-workflow.md) | Active |
| D015 | CSV bulk import kept in scope | The customer's data is currently in Excel; a product that cannot ingest it does not solve their problem | Active |
| D016 | Pay-equity analysis excluded | Needs protected-attribute data, a legal review and a privacy model that do not belong in a short build | Active |

## Data model

| ID | Decision | Why | Status |
|---|---|---|---|
| D017 | Salary as an append-only effective-dated history | Most interesting questions about pay are historical. [ADR-0002a](adr/0002-temporal-salary-model.superseded.md) | **Superseded by D018** |
| D018 | Current salary on the employee, plus an append-only audit log | The customer confirmed current state is sufficient, removing every capability that justified the temporal model. The log was kept because an unlogged mutation of pay data is a trust problem costing one table to avoid. [ADR-0002](adr/0002-current-salary-with-audit-log.md) | Active |
| D019 | `Employee.changeSalaryTo()` returns the `SalaryRevision`; no setter on `currentSalary` | Makes the audit guarantee structural — there is no code path that changes pay without producing a record | Active |
| D020 | Application DB role has `INSERT`/`SELECT` only on `salary_revision` | The append-only guarantee is enforced by the database, not only by the repository | Active |
| D021 | PostgreSQL 16, not SQLite | `numeric`, `percentile_cont`, `pg_trgm`, partial and covering indexes, per-table grants — all four things the schema leans on. [ADR-0003](adr/0003-postgres-over-sqlite.md) | Active |
| D022 | Keyset pagination, no `OFFSET` | O(1) at any depth and stable under concurrent writes. [ADR-0004](adr/0004-keyset-pagination.md) | Active |
| D023 | Trigram GIN index for name search | `ILIKE '%x%'` cannot use a B-tree; at 10k a sequential scan is fast enough to hide the mistake | Active |
| D024 | Native SQL projections for the dashboard, JPA for writes | Four KPI cards in one round trip; no entity loaded to be summed in Java. [ADR-0005](adr/0005-native-sql-for-analytics.md) | Active |
| D025 | Single tenant, no speculative `tenant_id` | Partially-implemented isolation is worse than none. [ADR-0010](adr/0010-single-tenant.md) | Active |
| D090 | The employee schema is `V2`, not the `V1` the plan said, and every later planned migration number shifts by one | `V1__enable_required_extensions` is already applied on every environment, and Flyway validates the applied history by version and checksum: reusing or renaming `V1` fails startup on the deployed instance and on any developer database that has run it. Renumbering the plan is free; renumbering an applied migration is an outage. PLAN.md carries the note so 2.2 and 2.4 do not rediscover it | Active |
| D091 | No column in the schema has a `DEFAULT` and no table has a trigger — not for identity, not for time | Two rules that are one rule. Identity is application-assigned (D067, D079): a `gen_random_uuid()` default is a second source of identity that mints rows the domain never created, and the seed generator holds ten thousand employees with their ids before the first insert. Time is passed in, never consulted (D075): a `now()` default or a stamping trigger reintroduces the ambient clock one layer below the port, where no domain test can see it — the rule does not stop at the boundary just because the boundary is where the code stops. `EmployeeSchemaIT` asserts both against `pg_attrdef` and `pg_trigger`, so a later migration cannot add one quietly | Active |
| D092 | `Department` is an inline natural-key value object in `shared`, not a `DepartmentId` referencing a department table | Reverses D071. There is no department aggregate for an id to reference — nothing in the system does anything to a department — so `DepartmentId` was identity for an entity that does not exist. Two concrete costs: a `bigint` surrogate is a second identity policy beside the application-assigned UUIDs (D091), and every query that reads the dimension pays a join for it — 3.2's four-KPI single round trip joining a table it reads nothing from, the department breakdown joining to fetch the label it groups by, 2.6's filtered search joining before it can filter with its index on a surrogate the UI never sends, and 2.10 resolving names to ids for ten thousand rows. It is the same kind of thing as `JobTitle` and `SeniorityLevel`, which are already value objects in `shared` on the same grounds (D082), and the filter dropdown is `SELECT DISTINCT department` off the btree index. The cost accepted: renaming a department is an `UPDATE` across its employees rather than one row, which is a rare operation on ten thousand rows and needs no lookup table to make it cheap | Active |
| D094 | `SalaryRevision` still carries no identity in the domain, but its surrogate is a `uuid` the persistence adapter assigns, not a `BIGSERIAL` | Supersedes D079's second half; its first half is unchanged. A sequence default is a database-minted identity, which is precisely what D091 rules out, and an exception for one table would be two identity policies in one schema. Minting it in the adapter rather than in `changeSalaryTo` keeps the aggregate a pure function of its arguments, which was D079's actual reason for having no id in the domain. The key also earns its place: it is the keyset tiebreaker for the revision list, where `changed_at` alone is not a total order | Active |
| D095 | The migration creates the application's database role itself — `salary_app`, `NOLOGIN`, no password — and grants it least privilege per table | The append-only guarantee is only real if the role the application connects as cannot `UPDATE` or `DELETE` a revision (D020), and a grant is meaningless without a named grantee. Creating the role in the migration keeps the privilege model versioned and reviewable as SQL alongside the table it protects, and `NOLOGIN` with no password keeps credentials out of the repository — attaching a login is provisioning's job. It also makes the guarantee testable: Testcontainers connects as a superuser, which bypasses every grant, so the tests `SET ROLE salary_app` and are refused exactly as the deployed application would be. Cost: the migration user needs `CREATEROLE`, which has to hold on Neon and is verified at 2.14 | Active |
| D096 | A `CHECK` lands only where it mirrors an invariant the domain already enforces, traced to the method and the test — so `previous_amount > 0` was dropped | `new_amount > 0` mirrors `changeSalaryTo`'s positivity guard and `new_amount <> previous_amount` mirrors its no-op guard, both with named tests. `previous_amount > 0` mirrors nothing: `Money` holds zero and negative amounts by design, and `Employee`'s constructor requires only a non-null salary in the right currency. A database-only rule is one the domain will eventually violate, and it would have surfaced while seeding ten thousand rows rather than in a test. **Open gap this exposed:** I10 reads "an employee's salary must be > 0" but the constructor does not enforce it, so `new Employee(..., zero)` is legal in Java and refused by `employee_salary_is_positive`. Recorded, not fixed here | Active |
| D098 | The `change_reason` CHECK and the `ChangeReason` enum are asserted to be the same set, read from `pg_constraint` | The CHECK duplicates the enum by necessity (D078), and duplicated lists drift. Read back rather than restated, the drift fails the build; restated in the test, it would pass while an insert failed in production for a reason the domain considers valid | Active |
| D100 | Every migration that creates a table grants it in the same file, and `SchemaGrantsIT` fails the build for any table in `public` that does not grant the application role at least `SELECT` | The application now runs as a role with nothing it was not given (D097), so a forgotten `GRANT` is no longer an oversight but a runtime failure — one with no good moment to be found, since the migration succeeds, the context starts, and the first query in the screen that needs the table returns "permission denied". Table and privileges reviewed as one change; `flyway_schema_history` is the single named exclusion, and a second test asserts it really is ungranted so the exclusion cannot quietly hide a grant. Enumeration reads `pg_tables` and the check uses `has_table_privilege`, because `information_schema` lists only tables the current user already has privilege on and would have hidden precisely the tables being looked for. Privilege shape: `salary_revision` `SELECT, INSERT`; mutable tables `SELECT, INSERT, UPDATE`; `salary_band` and `exchange_rate` `SELECT`; no `DELETE` anywhere without a stated need | Active |
| D101 | Both `DataSource` beans are declared explicitly, because declaring one backs off Spring Boot's | `DataSourceAutoConfiguration` is `@ConditionalOnMissingBean(DataSource.class)`, and the condition is on the **type**, not on the qualifier. Adding a single `@FlywayDataSource` bean — the documented way to give Flyway a different pool — therefore removed the auto-configured application pool, and the whole application silently ran on the migration pool: no `connection-init-sql`, no `SET ROLE`, connected as the owner of `salary_revision`, which holds `DELETE` on it whatever is granted. Nothing logged a warning; the context started; the schema's grant-catalogue test still passed, because what a role *may* do is unchanged by whether anything connects as it. What caught it was the behavioural test going through the real `DataSource`: three of its six assertions failed. `current_user` named the cause in one line; the two refusal tests reported only that a statement they expected to fail had succeeded | Active |
| D102 | Commit `980491e`'s claim that the bean mistake "passed every test in the suite; only asserting `current_user` caught it" is **false** and is recorded rather than quietly dropped | Three of the six append-only tests failed on that run, including both refusal tests. The true and narrower statement is that the suite *as it existed before 2.3* passed — the grant-catalogue assertion cannot see whether anything connects as the role — and that `current_user` shortened the diagnosis rather than performing the detection. Second occurrence of the species D057 named: not wrong code, which a test catches, but a wrong claim about verification, which reads like evidence and is checkable only by rerunning what it describes. This one propagated — it was repeated back as the project's strongest guardrail evidence and was about to be used as talk material. Logged as [08-AI-WORKFLOW](08-AI-WORKFLOW.md) entry 8 | Active |
| D103 | The seed runs on its own pool, without `SET ROLE salary_app`, and `exchange_rate` and `salary_band` grant the application `SELECT` only | Granting `INSERT` at runtime to accommodate a provisioning task inverts the relationship: the runtime privilege would be shaped by the seeder rather than by what the application legitimately does, and "read-only reference data" would quietly mean "read-only except for the one job that writes it". Seeding is provisioning and is therefore **a deliberate privilege escalation**, visible as a `seed`-profile `DataSource` a reviewer can see and object to, rather than an unexplained `INSERT` grant nobody reads. The runtime grant keeps saying `SELECT` and meaning it | Active |
| D104 | `pg_stat_database` counters do not count statements, and the N+1 gate drafted on them was deleted rather than shipped | `xact_commit + tup_returned` counts transactions and rows, so the assertion would have held at any number of queries — a gate that reports success while measuring nothing, the species this project keeps finding. Worse, `CLAUDE.md` already promises that "integration tests count statements and fail if the count exceeds what the test declares", which no code in the repository does: **the working agreement is currently making a claim the code cannot back**. The harness is built for real at 2.6, where a filtered query with a join is the first place an N+1 can actually appear, and `CLAUDE.md` is corrected in that same commit | Active |
| D105 | The seed draws its salary spread as scaled integer percentage points, never a `double` multiplier | First written as `BigDecimal.valueOf(0.88 + random.nextDouble() * 0.24)` and corrected before it ran. "It is only test data" is exactly how a float gets near money: these ten thousand figures are what 3.2 sums into total payroll spend and what 3.7b pins a full-precision reference against, so a rounding artefact seeded here would surface as a dashboard number nobody could reconcile | Active |
| D106 | The directory's first query pages with `OFFSET`, contradicting ADR-0004 deliberately and with an end date | The vertical slice existed to prove the whole path — schema, query, port, endpoint, screen — against ten thousand real rows, and the pagination strategy is not what it was proving. `OFFSET` reads and discards every row it skips and can repeat or drop rows when data changes between pages, which is why the ADR rejected it. It ends at 2.6, where keyset arrives with the filters and search that settle the query's shape; that commit states whether ADR-0004 is thereby satisfied or superseded | **Superseded by D108** |
| D107 | The directory's sort tiebreaker is `id`, a UUID: the order it imposes is arbitrary but total | Keyset paging needs a total order or it duplicates and skips rows at page boundaries, and `family_name, given_name` is not one — two people share a name. `id` breaks the tie. Because it is a UUID rather than a sequence, the resulting order carries **no meaning at all**: it is not insertion order, not seniority, not anything. Recorded so that nobody later reads significance into the order two identically-named colleagues appear in, or "fixes" it into something that is no longer a total order | Active |
| D108 | Keyset paging lands at 2.6 and **ADR-0004 is satisfied, not superseded** | The ADR's decision was keyset over `OFFSET` with an opaque cursor, and that is what shipped: the cursor carries the whole sort key `(family_name, given_name, id)`, base64url-encoded so no client parses it, and the page is `(family_name, given_name, id) > (?, ?, ?)` — exactly the rows returned, stable while people are hired mid-paging. D106's temporary `OFFSET` ends here | Active |
| D109 | `totalApprox` is an exact filtered count on the first page and absent on every page after it — a deviation from ADR-0004's `pg_class.reltuples`, recorded rather than hidden | The ADR's objection is that `COUNT(*)` runs on **every** page, and that objection is honoured: it runs once per result set. A screen still has to say how large the result is, and `reltuples` cannot answer that for a *filtered* query — it estimates the whole table, so "Showing 1–50 of 10,000" would appear above a filter matching six people. At ten thousand rows one filtered count off the same indexes costs milliseconds. The field keeps the name `totalApprox` because that is the freedom being preserved: when the count starts to hurt it becomes an estimate and no client changes, since none was ever promised an exact number. Not an ADR — switching the count strategy is a one-line change in one adapter, which is the opposite of expensive to reverse | Active |
| D110 | The application's datasource must be the **direct** database endpoint, never a transaction pooler, and `DatabaseIdentityCheck` refuses to start when either pool has the wrong identity | Production died on `permission denied for table flyway_schema_history` — a message only a `salary_app` connection can produce, raised on the migration pool, which is configured never to be one. `FlywayConnectsAsTheOwnerIT` proves the configuration is right and that Flyway uses it, so the connection was contaminated rather than the config: a transaction pooler shares one server connection between clients, and `SET ROLE` (D097) is session state that travels with it. The timeline confirms it — the first deploy that could fail was the first one where a migrated database and an existing `salary_app` coexisted, and its diff was two test files. **The dangerous direction is the other one**: an application connection inheriting the owner's session would leave the `salary_revision` grant binding nothing, silently, which is the audit guarantee gone. So the check fails the boot instead, naming the pooler; `spring.flyway.init-sqls: RESET ROLE` is defence in depth for the first transaction. The alternative fix — granting `salary_app` rights on `flyway_schema_history` — was rejected: it would let a contaminated migration read the history and then fail on the next `CREATE TABLE` anyway, and it treats the symptom | Active |
| D111 | `ChangeSalary` and `GetSalaryRevisions` live in the `employee` module, not in a `compensation` module as 03-ARCHITECTURE drew it | The revision is what the `Employee` aggregate returns, so a separate module owning the use case would have to import `employee.domain` — precisely what architecture rule 4 forbids and what 1.12 made executable (D087). The alternative, moving `SalaryRevision` into `compensation`, would make `employee.domain` depend on it and create the cycle rule 6 forbids. The aggregate and the use case that mutates it belong together; `compensation` was a name for a capability, not a boundary the code can hold | Active |
| D112 | The pay-change endpoint returns 400 for every rejection the domain makes, not the 422 and 409 that 04-API-DESIGN specifies | Distinguishing "you sent nonsense" from "the domain refuses this" means the domain raising typed rejections rather than `IllegalArgumentException` with a message, and that is a domain change with its own tests. It belongs with the RFC 7807 mapping rather than smuggled into a use-case task. What is returned today is documented in `openapi.yaml` as what it is, so the contract does not claim a code it does not send. Terminated employees already return 422, because `IllegalStateException` is distinguishable | Active |
| D113 | Nothing outside `..domain..` may construct a `SalaryRevision`, enforced by ArchUnit | The audit guarantee is that a revision can only come from `changeSalaryTo`. An adapter able to build one from an employee's before-and-after state would let any code path that moves pay produce a plausible record, and the log would keep looking trustworthy while no longer being derived from the only method allowed to change pay. Proved by making the persistence adapter mint one and watching the rule name the method | Active |
| D114 | The first version of the authentication test was decorative, and the deliberate break is what revealed it | Asserting `401` on an unauthenticated request passed **with `permitAll` on the directory route**, because `@PreAuthorize` at the use case still refused an anonymous caller and that refusal is also a `401`. Status alone cannot distinguish "the chain rejected it" from "the chain let it through and the use case caught it" — which is the defence-in-depth working and the gate measuring nothing, simultaneously. The test now sends `?limit=abc`, which fails Spring's parameter binding in the controller before any use case is consulted: a closed chain answers `401`, an open one answers `400`. Third instance of the species this project keeps finding, and the first where the vacuous gate was a **security** test | Active |
| D115 | `changed_by` is the authenticated principal's id, taken from the token subject; the request body has no actor field at all | A caller able to name the author of a pay change can attribute one to a colleague, which makes the audit log evidence of nothing. The token's subject is the user's id rather than their email precisely so it can be the foreign key the revision stores, with no second lookup and no room for the two to disagree | Active |
| D116 | The JWT signing secret has no default and the application refuses to start without one of at least 32 characters | A fallback secret in a repository is a published secret, and this one signs the tokens that authorise payroll changes. Failing at startup is recoverable; running on a key an attacker can read from GitHub is not. The local profile carries an obviously-fake key for development, in the same file and for the same reason as the Docker Compose password | Active |
| D117 | `ix_employee_filter` was specified by 05-DATA-MODEL, created, measured, and **not shipped** | The planner never chose it. With the partial `ix_employee_rollup` available the filtered directory query prefers that one — narrower, already excluding non-`ACTIVE` rows, fewer index pages to walk — and the plans with and without the covering index are the same shape. An index nothing chooses is write cost with no read benefit, so the migration omits it and the data model is corrected rather than the index being shipped to make a document true. Both plans are in `docs/evidence/03-filtered-country-department.*`. Trigger to revisit: the hundred-thousand-row comparison at 3.12, where the arithmetic changes | Active |
| D118 | Three vacuous gates in one project is a pattern, and it has three distinct shapes worth naming | **(a) Vacuous by construction** — `pg_stat_database` counts transactions and rows, so an N+1 assertion built on it holds at any number of queries (D104). **(b) Vacuous by wrong counter** — `information_schema` lists only tables the current user already holds privilege on, so a gate hunting ungranted tables is blind to exactly the tables it exists to find (08-AI-WORKFLOW entry 9). **(c) Vacuous by masking** — a `401` on an unauthenticated request cannot distinguish a closed filter chain from an open one whose downstream `@PreAuthorize` refused the caller (D114); this one was hidden *by a correctly implemented second layer of defence*, which is the most dangerous shape because the system is genuinely safe while the test proving it is worthless. Two structural lessons: a gate is only known to work once a deliberate break makes it fail, and **every suite of refusals needs a control that succeeds** — without "a valid token returns 200", three 401 assertions pass equally well against a chain that rejects everything | Active |
| D119 | The session lives in `sessionStorage`, and a `401` navigates to the login screen with a reason rather than emptying the table | A token that authorises payroll changes should not outlive the tab it was issued to, and there is no refresh or revocation to lean on — which is why it is thirty minutes. The failure mode being avoided is specific: an expired session that shows an empty directory says "nobody works here", which is a statement about the company rather than about the request. The interceptor ends the session and routes to `/login?expired=true`, and the screen says the session expired | Active |
| D120 | *(withdrawn — replaced by [ADR-0014](adr/0014-jdbctemplate-everywhere-jpa-on-a-read-path.md))* | Proposed as a log row saying "no JPA anywhere". It graduated: the reason is the audit invariant rather than a preference, it supersedes half of an accepted ADR, and it commits the project to answering the brief's JPA criterion elsewhere. Written as an ADR instead | **Superseded by ADR-0014** |
| D121 | Employee columns use the domain's vocabulary — `given_name`/`family_name` — not the ERD's `first_name`/`last_name` | `PersonName(given, family)` chose those words deliberately, and a table that disagrees with the aggregate makes every mapping a translation. `05-DATA-MODEL.md` was corrected to match, including the trigram expression, which must be written identically in the index and the query or the planner never considers it | Active |
| D122 | Free-text columns are unbounded `varchar`; only codes are fixed width | A length cap the domain does not have is a database-only rule nobody declared, and the first bulk import with a seventy-character job title would hit it. `char(2)` and `char(3)` stay for country and currency because those are fixed by their standards | Active |
| D123 | `app_user.role` has no `CHECK`, unlike `status` and `seniority_level` | Those two mirror enums the domain already had. There was no `Role` type when the table was created, and putting the authoritative list of roles in the schema first would make the database the source of truth for a domain concept. Now that `identity.domain.Role` exists the CHECK could be added — a candidate for the next migration that touches `app_user` | Active |
| D124 | The directory fetches `limit + 1` rows to decide whether a next page exists | Cheaper than counting and correct at the boundary where counting is not: exactly fifty rows and no fifty-first must not offer a cursor. The extra row is discarded before the page is returned | Active |
| D125 | `GET /employees/filter-options` derives dropdown values from the employees present | A department nobody works in cannot be chosen, and a new one appears without a deployment. Levels come from the enum instead, because that list belongs to the domain rather than to whoever happens to be employed. The column is selected by enum constant, never by a name from the request, so there is no path from input to statement. Not in `04-API-DESIGN.md`; it is in `openapi.yaml` | Active |
| D126 | JWT is HS256 with a shared secret, not RS256 | One service issues and validates its own tokens, so asymmetric signing would add key distribution to solve a problem nobody has. Trigger: the first service that must validate a token without being able to mint one | Active |
| D127 | CSRF protection is disabled | The API is stateless and authorises with a bearer token a browser does not attach automatically, so there is no ambient authority for a cross-site request to borrow. It needs reinstating the moment anything moves to a cookie | Active |
| D128 | Salaries are displayed with no decimal places | `maximumFractionDigits: 0`, so ₹1,330,000 rather than ₹1,330,000.00. Annual pay is compared in thousands and the cents are noise on a directory row. Nothing is lost: the API sends a string and the pipe only formats | Active |
| D129 | Demo credentials are published in the README and seeded with BCrypt hashes | A reviewer must be able to sign in without asking, and fixed user ids mean a revision written in one demo still names a user after the next seed. Hashed even in the seed: a demo that stores plaintext teaches the next person that this is a place plaintext goes | Active |
| D130 | `V2` and `V3` were edited after being committed, rather than superseded by new migrations | Normally forbidden — D090 exists because of it. Permitted here because they had been applied only to ephemeral Testcontainers databases and the commits were unpushed, both verified before editing. The rule stands unchanged for anything that has reached a durable database | Active |
| D131 | "Before" query plans are captured with `BEGIN; DROP INDEX …; EXPLAIN …; ROLLBACK;` | Truthful and cheap: the index really is absent for the measurement, nothing is rebuilt, and the database is unchanged afterwards. Two queries also have a `.no-indexes` capture, because "before this index" and "before any index" answer different questions | Active |
| D132 | FX rates are seeded by the migration; bands and revisions by the seed generator | Five fixed rows identical in every environment are what a migration is for, and seeding them there means the application needs no `INSERT` on `exchange_rate` in any profile — so the `SELECT`-only grant is the whole truth rather than the truth outside seeding (D103). Bands and revisions cannot go the same way: they are generated from the same pay scale as the employees and must change when that does | Active |
| D133 | Seeded revisions are produced by `changeSalaryTo`, not written directly | The seeder walks each employee forward through two to four raises and collects what the aggregate returns, so thirty thousand seeded audit records are thirty thousand the domain agreed to — no no-ops, no currency mismatches, no unexplained figures. It also means an employee's final salary is where their own history ended, rather than a number with a history invented around it. The alternative, inserting plausible rows, would have made the seed the one place in the system that can manufacture an audit record | Active |
| D134 | Bands are derived from the same pay scale the salaries are drawn from | Bands invented independently would put most of the company outside its own approved ranges, and a compa-ratio column saying everyone is underpaid is a column nobody looks at twice. Midpoint is the market rate for that level; the range is 0.85 to 1.20 of it, which comfortably contains the ±12% spread the salaries are generated with | Active |
| D135 | Money percentiles use `percentile_disc`, never `percentile_cont` | `percentile_cont` interpolates, so PostgreSQL returns `double precision`: against the seeded data the median comes back as `113930.00993500001`, a float in a payroll figure, which `CLAUDE.md` forbids outright. `percentile_disc` returns `numeric` and returns an amount somebody is actually paid, which for a median salary is the more truthful answer as well as the exact one. `05-DATA-MODEL.md` specified `percentile_cont` and is corrected. Applies to the p25/p75/p90 distributions at 3.4 too | Active |
| D136 | `Money.convertTo` is deleted, settling D039's probation | The condition was "no production caller by end of Day 3". The dashboard query normalises in SQL, as ADR-0013 said it would, so the method finished the build with ten test callers and none in `main`. Deleted with the eight example tests and the property test that existed to exercise it. What is lost: the Java-side demonstration of ADR-0013's derived bound (D060). What replaces it: a measurement in `docs/evidence/08-kpi-summary.txt` showing per-row conversion and sum-then-convert agreeing **to zero drift** in PostgreSQL `numeric`, which is where the rule now actually lives. The ADR stands unchanged; only the unused API is gone | Active |
| D137 | Cross-currency conversion in SQL is per row, and ADR-0013 still holds | The ADR prohibits per-amount conversion because `Money.convertTo` rounded to the currency's scale on every call, so a total built from rounded parts drifts by roughly half a minor unit per row. `numeric` multiplication is exact and nothing rounds until the end: summing per currency and converting once gives `1213179157.200437000000`, converting per row gives the same digits, drift exactly `0.000000000000` — measured, not assumed. Per-row is also required rather than preferred, because a median cannot be computed from subtotals | Active |
| D097 | The application's own connections run `SET ROLE salary_app`; the migration grants the login user membership in it | D020 was decorative without this. `salary_app` is `NOLOGIN`, so nothing connected as it: the application connected as the owner of `salary_revision`, which holds `DELETE` on it whatever the grants say. Membership plus `spring.datasource.hikari.connection-init-sql` makes the restriction apply to the running system, and 2.3 proves it through the application's own `DataSource` rather than by issuing `SET ROLE` itself — a test that grants itself the privileges it is testing is the vacuous-gate pattern in a new costume. Consequence: migrations need their own pool, because on a fresh database the role does not exist when the first connection opens and a migration running as `salary_app` could create nothing. Both pools are declared explicitly, because declaring one `DataSource` bean backs off Spring Boot's — which first left the entire application running on the migration pool, restriction applied to nothing | Active |

## Domain — Money and currency

| ID | Decision | Why | Status |
|---|---|---|---|
| D026 | `Money` as a value object, `BigDecimal` only, cross-currency arithmetic throws | Six currencies and every dashboard total crosses them. [ADR-0006](adr/0006-money-value-object.md) | Active |
| D027 | `Money` permits zero and negative; `minus` is total | Reverses an error in the domain doc. Two concrete bugs the alternative causes: `Money.zero(USD)` for an empty dashboard filter, and `salary.minus(band.min())` throwing for exactly the below-min employees the feature exists to surface | Active |
| D028 | I1 reattributed from `Money` to `Employee.changeSalaryTo` + DB `CHECK` | "A salary must be positive" is a rule about salaries, not about money. Naming a positive-only type `Money` is a lie | Active |
| D029 | Scale from `java.util.Currency.getDefaultFractionDigits()`, not a hardcoded map | Correct for every currency, no second source of truth, and gives ISO-4217 validation on `CurrencyCode` free | Active |
| D030 | Reject currencies whose fraction digits are −1 | Pseudo-currencies (XAU, XDR, XXX) would otherwise reach `setScale` with −1 | Active |
| D031 | `Money.of(String, ...)` and `Money.of(BigDecimal, ...)`; no `double`/`float` overload | `new BigDecimal(0.1)` is a real trap. Absence of the overload is a stronger guarantee than a lint rule | Active |
| D032 | Compact constructor normalises scale with HALF_EVEN rather than rejecting | Arithmetic results must round somewhere; rejecting forces callers to round first, which is where rounding bugs live. Over-precise user input is rejected at the DTO edge | Active |
| D033 | `convertTo` deferred from 1.7 to 1.8 | A minimal `ExchangeRate` in 1.7 reshaped in 1.8 means two commits on one type where the first is throwaway | Active |
| D034 | `isGreaterThan` written, then deleted; `isPositive()` only, arriving with I1's test in 1.10 | No test demanded it. Narrowest method the test requires | Active |
| D035 | No Java-side dated rate lookup in 1.8 | Normalisation happens in the SQL join; a Java rate table would be code the dashboard never calls. Date resolution stays behind the `ExchangeRateProvider` port for 2.x | Active |
| D036 | `convertTo` rejects a wrong-direction rate rather than inverting it | "No implicit conversion" covers direction. Silent inversion turns a bad seed row into plausible-but-wrong numbers, and `1/rate` adds invisible rounding error | Active |
| D037 | Same-currency conversion returns `this`, no rate required | Identity is not conversion. Otherwise the compa-ratio path needs INR→INR rows nobody will seed | Active |
| D038 | `ExchangeRate` rejects a rate ≤ 0 | A zero rate silently zeroes a payroll total | Active |
| D039 | `Money.convertTo` on **probation** — delete if it has no production caller by end of Day 3 | Normalisation is SQL-side, so it may never be called. It exists now because the rounding policy needs somewhere to live. Same standard applied to `isGreaterThan` (D034) | **Resolved by D136 — deleted** |
| D058 | Cross-currency totals are summed per currency and converted once; per-row conversion is prohibited | [ADR-0013](adr/0013-sum-then-convert-for-currency-totals.md) | Active |
| D059 | Two properties guard it: P1 exact and order-independent, P2 bounded and named as a prohibition | [ADR-0013](adr/0013-sum-then-convert-for-currency-totals.md) | Active |
| D060 | The documented "within one minor unit" property bound was arithmetically false; the real bound is `0.005 × (n + currencies)` | A fixed one-unit bound holds only for lists short enough not to expose it, and jqwik finds the counterexample immediately. [ADR-0013](adr/0013-sum-then-convert-for-currency-totals.md) | Active |

## Domain — Employee

| ID | Decision | Why | Status |
|---|---|---|---|
| D067 | `EmployeeId` is assigned by the application at construction, never generated by the database | A null id before persistence breaks equality for the entity's whole transient life — which bites in the seed generator, holding ten thousand unsaved employees before the first insert | Active |
| D068 | `CountryCode` rejects an ISO-3166 country the JDK has no currency for | Narrows valid input deliberately: Antarctica is a real country code with no currency, and rejecting it at construction means `currency()` never returns null or throws | Active |
| D069 | `EmailAddress` is lowercased at construction | Equality becomes case-insensitive and the stored value can differ from what was typed. Correct for email, but a decision rather than an accident | Active |
| D070 | `Employee` carries `department`, `jobTitle` and `seniorityLevel` from 1.9; `employmentType` and `managerId` deferred | The first three are the filter dimensions the customer named (D010) and every directory and dashboard query groups on them — a known consumer 24 hours away is scope, not speculation, and the reshape would otherwise land when there is least slack. The other two are display-only; adding a column later is a migration, not a reshape | Active |
| D071 | `Employee` references its department by `DepartmentId`, not by a `Department` value | An aggregate references another aggregate by identity, never by object reference — the class diagram already modelled `managerId` as an `EmployeeId` and broke its own rule for department. `05-DATA-MODEL.md` has `department_id` as an FK, and the id keeps a department name from being copied onto ten thousand employees. The diagram is corrected, not diverged from | **Superseded by D092** |
| D072 | The test for I1, `changing_salary_to_zero_is_rejected`, lands with 1.10 rather than 1.7 | I1 moved off `Money` and onto `Employee.changeSalaryTo` (D028), so its test belongs where the rule now lives. Carried over from the root `STATUS.md` before that file was deleted | Active |
| D040 | I8 (terminated employee) enforced in `Employee`, not `ChangeSalaryUseCase` | The aggregate holds `status`, so it can enforce the rule without a collaborator — same basis as I6 and I9. In the use case, bulk import or any second entry point could bypass it | Active |
| D075 | `changeSalaryTo` takes the `Instant` to record, not a `Clock` | Reverses an earlier instruction that named the mechanism instead of the goal. A `Clock` parameter is still a time source, so the method's result depends on when it runs — non-determinism inside the aggregate, merely parameterised. An `Instant` makes it a pure function of its arguments; the application layer resolves `Clock` to `Instant` at the boundary. `CLAUDE.md` now states the goal | Active |
| D076 | `note` is on `SalaryRevision` and in the signature from 1.10 | Same basis as the filter dimensions (D070): it is in `04-API-DESIGN.md`, on the entity in the domain model, and has a confirmed consumer at 2.7 tomorrow. Nullable, no invariant — the reason is the mandatory part | Active |
| D077 | `ChangeReason` is `MERIT`, `PROMOTION`, `MARKET_ADJUSTMENT`, `CORRECTION` | Each has a real producer: the seed generator emits all four. `HIRE`, `DEMOTION` and `ROLE_CHANGE` have none — the initial salary is set at construction and correctly produces no revision, so a `HIRE` reason would name an event that never reaches the log | Active |
| D078 | `change_reason` is stored as `varchar` + `CHECK`, not a PostgreSQL native enum | Decided now so 2.2 does not have to revisit it. Adding a value to a native enum is a migration with locking behaviour; extending a `CHECK` constraint is cheap, and the set will grow the first time HR names a reason nobody anticipated | Active |
| D079 | `SalaryRevision` carries no identity; the database gives it a surrogate `BIGSERIAL` | Entities that are referenced need identity; append-only records that are only ever listed do not — a revision is never looked up, updated, deleted or pointed at, and its only endpoint is a list. Removing it also keeps `changeSalaryTo` a pure function of its arguments: minting a UUID inside the aggregate would have broken the seed generator's promise of identical data across runs (05-DATA-MODEL §6) for some thirty thousand revisions. `Employee` keeps its application-assigned id (D067) for the opposite reason — it is referenced | **Superseded by D094** |
| D080 | Domain exception messages name the employee and the currencies, never an amount | `CLAUDE.md` forbids salary figures in log output, and an exception message is the most likely thing in the system to be logged — a handler logging `getMessage()` would put a named employee's exact pay in the application log. `04-API-DESIGN.md` line 85 already modelled the right shape | Active |
| D081 | `note` is trimmed and a blank note recorded as no note | Consistent with `EmployeeNumber` and `JobTitle`, which already trim. `"   "` stored as whitespace is a value that reads as present and means absent | Active |
| D041 | I8 enforced on `status == TERMINATED`, not `terminationDate` arithmetic | Date arithmetic drags a `Clock` into the check and raises a future-dated-termination question nothing in scope needs answered | Active |

## Domain — Bands

| ID | Decision | Why | Status |
|---|---|---|---|
| D082 | `JobTitle` and `SeniorityLevel` moved from `employee/domain` to `shared` | `SalaryBand` needs both, and a module reaching into another module's domain breaks architecture rule 4. **Guard: a type moves to `shared` only when two or more modules genuinely consume it** — employee, band and analytics all group by these two. Without that test the kernel becomes the place everything goes | Active |
| D093 | D082's "two or more modules consume it" is read as **known consumers in committed scope**, not modules whose code exists today | Settled here so 3.3 does not re-argue it. `Department` joins `JobTitle` and `SeniorityLevel` in `shared` on identical standing: `analytics` groups its breakdown by all three, and that consumer is a day away in committed scope, which is the same basis on which the other two moved during 1.11. Reading the guard as "code that exists right now" would leave the third of four sibling filter dimensions in `employee/domain`, where `analytics` could only reach it by importing another module's domain — the precise thing architecture rule 4 forbids and 1.12 made executable (D087). The guard's purpose is to stop `shared` becoming the place everything goes, and a named consumer in committed scope is not everything | Active |
| D099 | `Employee`'s constructor enforces I1, reusing the same guard `changeSalaryTo` uses. Supersedes D096's decision to record the gap rather than close it | Recording it was the wrong call in this direction. A `CHECK` with no domain rule behind it produces rows the domain cannot write; a domain with no guard behind a `CHECK` produces objects the database cannot store, which is worse — the aggregate was constructible in a state that could never be persisted, so every unit test that built an `Employee` asserted against a looser object than the one that ships. The bite was timed: one seed-generator edge producing zero would have surfaced at 2.10 as a constraint violation mid-batch, not as a named constructor test. One `requirePositive` used by both entry points, so positive has one definition | Active |
| D083 | `SalaryBand` carries no `BandId` | Same principle as D079: found by the natural key of role, level and country, which `05-DATA-MODEL.md` already makes unique, and referenced by nothing. `BIGSERIAL` PK in the table, absent from the domain. The class diagram is corrected, as it was for `DepartmentId` (D071) | Active |
| D084 | Compa-ratio is held to 4 decimal places, `HALF_EVEN` | `1234567 / 1250000` recurs and must round somewhere: precise enough to sort a directory by, not so precise it implies accuracy the inputs lack | Active |
| D085 | All three band bounds must be greater than zero, not only the midpoint | Rejecting only the divisor would be a rule shaped by the implementation. A band with a zero minimum describes no range anyone could be paid within, so I10 now reads `0 < min ≤ mid ≤ max` | Active |
| D086 | Bounds are checked before the compa-ratio when classifying | Outside the range is a different statement from far from the midpoint: an employee under the minimum is `BELOW_MIN` whatever their ratio, and a band whose min sits below 0.9 of mid would otherwise report `LOW` for someone genuinely out of range | Active |

## Quality gates

| ID | Decision | Why | Status |
|---|---|---|---|
| D042 | Quality gates wired into the build before any feature code | A JaCoCo or Pitest threshold is easy to satisfy from zero and painful to retrofit | Active |
| D043 | Gates must be *proved* to fail, not assumed to work | A gate that has never failed is decoration. Three gates were found reporting success while checking nothing | Active |
| D044 | Mutation scope extended to `shared` | `Money` lives in `shared`; the gate targeted `domain` and `application` only, so it reported zero mutations and passed vacuously while claiming the pay arithmetic was verified | Active |
| D045 | Framework-freedom rule changed from a denylist to an allowlist, covering `domain` **and** `shared` | Proved by putting `@Component` in `shared` and watching all eight rules pass. Domain imports shared, so framework code reached the domain through the one package the rules did not check. A denylist also misses anything unenumerated — slf4j was on the classpath and unnamed | Active |
| D046 | `failOnEmptyShould=true` scheduled as the final task of Day 2 | Five of the eight rules match zero classes because their packages do not exist yet — legitimate now, but a later package rename would silently disable a rule forever | Active |
| D047 | PMD `MissingSerialVersionUID` excluded for domain exception types, scoped, with the reason in the ruleset XML | These exceptions are never serialized. A `serialVersionUID = 1L` nobody will ever bump is a false maintenance signal, worse than absence | Active |
| D087 | Architecture rule 4 (modules do not reach into each other's internals) made executable | Documented since Day 0 and unenforced until 1.12: the framework-freedom allowlist permits any `..domain..` to depend on any other, so `band` importing `employee/domain.JobTitle` would have passed silently. The sixth gate found reporting success while checking nothing. Proved by making `SalaryBand` return an `EmployeeId` and watching "module band depends on module employee" fail the build | Active |
| D088 | `requireNonNull` guards are **not** converted to explicit `if`-throw today, though it would make them mutable | The example tests cover those guards; it is the measurement that is incomplete, not the coverage. Recorded with the distinction that matters: converting them later would be legitimate **because it improves the design on its own merits** — an NPE is a JDK exception leaking out as a domain validation failure — and only incidentally makes the guard mutable. That is the opposite of the rejected static-factory option (D063), which would have made the invariant bypassable to suit a tool | Active |
| D089 | A test audit is run against the domain before it is built on, not after the build is finished | Ten mutations applied, eight survived: the suite was strong on everything the session had argued about and weak on everything nobody wrote a paragraph about. I9 in particular passed one hundred and four tests with the currency hardcoded, because every employee in the suite was Indian and the seeder does six countries tomorrow | Active |
| D048 | Mutation testing at 70% on domain, with JaCoCo ungated on adapters | Line coverage says a line ran; mutation coverage says a test would notice if it were wrong. Chasing coverage on generated mappers produces tests nobody should read | Active |
| D049 | Testcontainers PostgreSQL rather than H2 for tests | Tests must exercise the engine, dialect and constraints that production uses | Active |
| D050 | Add a Day 3 test asserting the SQL aggregate and a Java computation agree to the minor unit | The jqwik property proves a rounding policy PostgreSQL is not bound by. Without this the property stays green while the dashboard reports a different total | **Superseded by D061** |
| D061 | PLAN 3.7b asserts **exact** equality against a full-precision `BigDecimal` reference that mirrors the SQL — accumulated across all currencies unrounded, rounded once at the end — and the reference must not use `Money` | "Agree to the minor unit" (D050) was still a tolerance, and 0.005 × currencies is three cents at six currencies, not one. `Money` rounds on construction (D032), so it represents amounts and cannot be an accumulator | Active |
| D138 | The mutation gate is re-measured whenever the domain changes, and the README quotes the measurement rather than the threshold | It had not run since Day 1, over a domain that had since gained `Department` and `requirePositive` and lost `Money.convertTo` with ten tests. Re-run: 178 of 201 mutations killed, **89%**, test strength 96%, against a 70% threshold. The README said "Pitest at 70% on the domain", which reads as the score and is the floor — the same shape of overstatement as D104's statement about a harness that did not exist. It now quotes 89% and links to what the score cannot see (D064) | Active |
| D139 | PLAN 2.15 (`failOnEmptyShould=true`) is deliberately left unmet, and no empty packages are created to satisfy it | The gate is honest only if the five module packages exist because the code needed them. `compensation` does not exist and may never — D111 put the change-salary use case in `employee`, where the aggregate is — and `bulkimport` is unbuilt. Creating either as an empty package would make ArchUnit pass by manufacturing the structure it is meant to be checking, which inverts the rule the whole project runs on (D043: a gate must be proved to fail, not decorated). An unmet gate with a stated reason beats a green one with hollow packages behind it. Revisit when `bulkimport` lands at 3.10 | Active |
| D140 | A dashboard answer is painted only if it answers the filter currently on screen | Two filter changes in quick succession can return in either order, and the slower answer is the older question. Painting it puts one filter's figures under another filter's controls — 412 people under a filter bar reading "Any country" — and a stale *failure* arriving after a fresh success would erase figures that had loaded. The facade tags each request and drops answers to superseded ones; two specs hold the interleaving open with subjects rather than asserting the sequential case. Found by review, not by a test, which is why the specs came with the fix | Active |
| D141 | An explanatory comment is an unverified claim, and is read with the same suspicion as a document | All three defects the 3.9 review found had a comment directly above them asserting they were correct. The facade's said its shape ruled out mismatched figures — it ruled out the four cards disagreeing with each other, not the card set disagreeing with the filter bar (D140). The `aria-labelledby` comment said it was how a screen reader finds the value, when the attribute is prohibited on `role="paragraph"` and ignored; it existed to give a spec a selector. Both api services explained stripping blank parameters because "the API reads a blank string as a filter matching nobody", which the running API contradicts with a 400. Comments are the least reliable artifact in this repository for a structural reason: nothing executes them, so nothing can prove them false — a test that lies fails, a comment that lies is read and believed. When a comment states what the system does, verify it the way the claim would be verified if it were in a document | Active |
| D142 | A review reads the claim, not the diff — including the lines the diff did not touch | The README said "integration tests assert the query plans use indexes"; no test in `src/test/java` runs `EXPLAIN`. Fourth instance of the D104 shape, and the distinct one: `4e5e9e4` rewrote that exact bullet to correct the percentile rule two commits earlier and left the false sentence standing directly above the line it changed. Attention was scoped to what was being edited rather than to what the paragraph asserted. A README claiming tests that do not exist is the cheapest lie for a reviewer to disprove — `grep -r EXPLAIN src/test` is thirty seconds — and it discredits the claims either side of it that are true | Active |
| D143 | The breakdown reads the FX date in a second statement rather than as a scalar subquery | One grouped query cannot return a date when it returns no groups, and a filter matching nobody still has to say which day it would have converted through — otherwise the one case where the screen shows least is also the one case it explains least. Two constant statements is not an N+1: the property that matters is that the count does not grow with the rows, and the test pins exactly that by measuring two different group counts and asserting the same number. The KPI summary keeps its single statement because it always returns exactly one row | **Superseded by D144** |
| D144 | The snapshot date rides along on every grouped row; the extra statement is only for the empty case | D143 made it unconditional, which cost every rendered dashboard a second round trip so that the one case rendering nothing could name its rates. Now the grouped queries select the date as a column, so the populated path — every path a dashboard actually draws — is one statement, and the separate read happens only when there are no rows to carry it. Both grouped queries pin both counts - populated at 1, empty at 2 - so neither can drift into the other. The claim was written before the distribution had its empty-case count; the review caught that it was true of one query and stated of two | Active |
| D145 | An unconvertible salary refuses the whole answer rather than converting at 1.0 | [ADR-0015](adr/0015-refuse-to-convert-rather-than-assume-a-rate.md). Found by review, reproduced against the seeded database: `?currency=EUR` reported India at EUR 4.93bn, and a single newer rate row moved global payroll from 1.21bn to 6.19bn on the default path. A number an HR manager would act on and could not question is the worst thing this product can emit | Active |
| D146 | The three analytics adapters share one normalisation CTE | The copy was the defect. `COALESCE(fx.rate, 1)` was written once, then pasted twice, and by the time it was found it was wrong in three files — including one written after the review that found it. Rule of three, applied to the thing that actually hurt: not the shape, the arithmetic | Active |
| D147 | Optimistic locking is a compare-and-set on the salary itself, not a version column | The value being guarded is the value being written, so a version column would add a column and a migration to say what `salary_amount` already says — and a stale version is harder to reason about than a stale salary. `WHERE id = ? AND salary_amount = ? AND salary_currency = ?` with the revision's own `previousAmount`, rejecting on zero rows. Currency is in the predicate as defence-in-depth rather than for a reachable case: I9 pins salary currency to the employee's country and `country` is final, so a currency-only move cannot be expressed today - the clause is there for the day that is relaxed, and is deliberately untestable until then. What a value CAS does not close is ABA: 100k -> 120k -> 100k and a write decided against the first 100k succeeds. The revision it writes is still truthful, so the audit log stays sound; what the manager loses is the knowledge that they were looking at stale state, which a version column would have caught and this does not. Proved by reverting the clause: two threads then both succeed and the log carries two revisions from the same starting salary (`expected: 1 but was: 2`), which is D043's standard — a gate must be proved to fail | Active |
| D148 | A concurrency test must assert a property that holds under every schedule, not the schedule it happened to get | The threaded lock test had both managers target the same figure, so a run where one finished before the other read made the second a no-op - refused for a reason unrelated to the guard, uncaught, and green for the wrong cause until it errored intermittently in the full suite. It now asserts what must be true either way: no two revisions may claim the same starting salary. Interleaved, one is refused and one revision exists; serialised, both succeed from different figures; unguarded, two revisions both start at 100,000 | Active |
| D149 | A test that catches a supertype of the expected failure proves less than its name | The same test caught `ConcurrentSalaryChange` **or** any `DataAccessException`. A guard regressing into a lock timeout or a constraint violation would have kept it green while `PUT /salary` returned 500 instead of 409, because neither has a `ProblemHandler` mapping. Catch the one exception the behaviour is named for | Active |
| D150 | The compare-and-set was verified at READ COMMITTED by running it, not by reasoning about it | An interleaved `UPDATE ... WHERE amount = ?` issued against PostgreSQL 16 while another transaction held the row uncommitted **blocks**, re-evaluates its predicate after that commit, and reports `UPDATE 0` - no lost update and no serialization error. Isolation-level arguments are exactly the kind of claim D141 says to distrust until executed | Active |
| D151 | A fixed bug must be un-documented everywhere it was described, not only where it was fixed | ADR-0014 still said there was no optimistic locking and prescribed a `version` column, and `openapi.yaml` sent readers to that ADR for the explanation. Following that trail produces two guards that disagree on top of a working one. The ADR is amended in place - what it claimed, that it was true when written, what happened instead - because an append-only log of decisions is worth nothing if a superseded one still reads as current | Active |
| D152 | The JPA slice is the revision **read** only, and the reading port is separate from the appending one | ADR-0014 promised one mapped read once the dashboard shipped. `SalaryRevisionLog` now only appends and `SalaryRevisionReader` only reads, so the line the whole write-side argument rests on is in the type system rather than in a comment: the port that can write cannot read, and the port on JPA cannot write. No entity is ever attached for mutation and there is no setter on either entity | Active |
| D153 | An N+1's cost scales with distinct associated rows, not with rows read - measured, after the first fixture hid it | The first version gave all thirty revisions the same author and the naive read cost **2** statements, not 31: Hibernate loads that user once and the persistence context answers the rest. With thirty distinct authors it is 31, and the entity graph makes it 1 either way. All three numbers are asserted, because the 2 is the one that would have let an N+1 through review - a fixture with one author makes a linear cost look constant. `docs/evidence/09-jpa-read-path.txt` | Active |
| D154 | A Spring Data interface inside an adapter is not a port, and is not named as though it were | ArchUnit failed the build on `SalaryRevisionJpaRepository` residing outside `application.port.out`, which was the rule working: in this codebase a `Repository` is declared by the layer that needs it. The interface is an implementation detail of one port, so it is `SalaryRevisionRows` and the rule stands unweakened. Renaming the code to satisfy an honest gate is the opposite of D139's hollow packages | Active |
| D155 | Seeded bands are set around what people are paid now, not what they were hired on | The midpoints are a starting scale and the seed then walks everybody through two to four raises, so six employees in ten came out above their own maximum - which reads as a broken band rather than a well-paid organisation, and made the new band display look wrong on its first screenshot. Midpoints are lifted by the same progression the population applies. Measured after: 65% within band, 14% high, 12% low, 7% below minimum, 2% above maximum | Active |
| D156 | The salary input is a text field with a regex, never `type="number"` | Angular binds a number input through `NumberValueAccessor`, which is `parseFloat` - money through a double, in a codebase that forbids exactly that on the wire. It also broke typing: at the keystroke `.` the browser reports an empty value for `1500000.`, the one-way binding wrote that back and the field cleared itself mid-entry. Reproduced character by character in a real browser before reverting. `inputmode="decimal"` keeps the phone number pad, which was the only real benefit | Active |
| D157 | Placing a salary in its band is a POST, because the question contains pay | A query string is the one part of a request that reaches the access log, the browser history and the `Referer` of everything the page loads next. "No salary figures in log output" is a rule about where pay ends up, not about which logger wrote it, so the figure travels in a body. Nothing is created; the 200 is the answer, not a resource | Active |
| D158 | A failed read is never rendered as an answer | A band request that errored set the same null as "this role has no band", so the dialog stated "No band defined for this role and level" - an affirmative claim about the organisation, made on the strength of a failed request. Three states now: loading, absent, failed. The same distinction the dashboard already draws between zero and unavailable | Active |
| D159 | The change-pay dialog closes on 409 and on 401, and stays open on everything else | A refusal the manager can act on belongs where they are looking, with what they typed intact. A conflict is the opposite: the message says to decide against the figure now on record, and that figure is on the screen the dialog is covering. A 401 is neither - the interceptor is already navigating to sign-in, and "the change was refused" over a sign-in screen is a message about the wrong thing | Active |
| D160 | A deploy is verified after the new instance is serving, not after the old one answers | The seed profile added to `render.yaml` **did** work: Neon now holds 29,926 revisions, 510 bands and a 1,213,179,157.20 total. I measured while Render was still serving the previous container - which it keeps up until the replacement is healthy - read the pre-seed figures, and concluded from them that blueprint env vars are not applied on auto-deploy. They are. `32dcc6f` reverts the profile for the right reason (left on, every cold start truncates the demo) and states a wrong one. The check that would have caught it costs nothing: ask the deployed build for something only the new code answers before trusting any number it returns | Active |
| D161 | `ng test` is not a build: the production compiler has to run before anything is pushed | The UI deploy never landed today and I spent two checks wondering why the bundle hash was unchanged. `ng build` was failing on strict template type-checking - `bandPosition(string \| undefined)` handed a `null` after the band fields were correctly typed nullable - while all 101 specs passed, because Karma compiles JIT with looser template checks than AOT. So the suite was green, the commit looked finished, and the deployed site silently stayed on the previous build. `ng build` now runs before any push that touches the UI, and belongs in CI beside the Angular suite that does not run there either | Active |

## Working method

| ID | Decision | Why | Status |
|---|---|---|---|
| D051 | Tests written and approved before implementation, every task | AI output is accepted because a test passes, never because the code looks plausible | Active |
| D052 | One commit per task; gate fixes get their own commit; doc changes describing the same behaviour fold in | The history is graded. A gate defect exists independently of the feature that exposed it; an invariant's doc line does not | Active |
| D053 | Gate fixes ordered *after* the code that makes them non-vacuous | A gate-fix-only commit would have failed on "no mutations found" — a red intermediate commit is worse than imperfect ordering | Active |
| D054 | `/prime` at the start of every session, `/wrap` at the end | `docs/STATUS.md` is the handoff; without it each session re-derives context | Active |
| D055 | `git log` wins where it disagrees with `STATUS.md` | The status file is hand-written and drifts; commits cannot | Active |
| D056 | Every rejected or corrected AI output recorded in [08-AI-WORKFLOW.md](08-AI-WORKFLOW.md) | The failures are more informative than the successes, and four of the first six became build rules rather than reminders | Active |
| D057 | An overstated verification claim logged as a distinct failure mode | Wrong code is caught by a test; a wrong claim about what was verified reads like evidence | Active |
| D063 | Validation stays in `Money`'s compact constructor; moving it to `Money.of()` to make it mutable-by-Pitest was rejected | `Money` is a record, so the canonical constructor is public: `new Money(rawBigDecimal, INR)` would bypass scale normalisation entirely. That trades an unmeasured invariant for a bypassable one — a correctness regression, and the reason is the bypass, not that the alternative merely suited a tool | Active |
| D064 | A mutation score is bounded by what the tool elects to mutate, and `07-TEST-STRATEGY.md` §5 says so | Pitest mutates no record compact constructor unless `-FRECORD` is set; even then it attributes no killer to coverage arising from a test class's static initialiser, and `Money`'s rounding policy has no mutable construct at all. The README quotes the number, so the number must carry its limits | Active |
| D065 | Pitest runs with `-FRECORD`, accepting uncovered mutations in record-generated methods | With the filter on, `ExchangeRate` generated zero mutations and the gate reported 100% having tested none of it. Off, the real constructor guards are mutated and killed and the score is 80% against a 70% threshold; the cost is junk survivors in `equals`/`hashCode`/`toString` | Active |
| D066 | `Money`'s rounding policy verified by a recorded manual probe rather than left unverified | No default mutator applies to `setScale(currency.scale(), HALF_EVEN)` — no conditional, no operator, no return value. `HALF_EVEN`→`HALF_UP` fails 3 of 4 rounding tests and `scale()+1` fails three nested classes; both are recorded in §5 and re-run when the policy changes | Active |
| D073 | PMD's `AvoidFieldNameMatchingMethodName` excluded, scoped with the reason in the ruleset | Accessors named after their field are this codebase's convention because that is what a record generates. PMD cannot see generated accessors, so the rule only ever fires on the one aggregate written by hand — renaming `Employee.id()` to `getId()` would make it the odd one out | Active |
| D074 | Pitest excludes `equals`, `hashCode` and `toString` from mutation | `-FRECORD` (D065) surfaces compact-constructor guards, which is the point, but it also surfaces every record's generated methods: eight new records added roughly forty mutations nobody writes tests for and the score fell to 58%, below the threshold. Excluding those three names keeps the guards mutated and the noise out; the cost is that `Employee`'s hand-written identity equality is excluded too, and is covered by two example tests instead | Active |
| D062 | Every numeric tolerance is derived and shown before it is accepted, never asserted | Three tolerances were specified and all three were wrong: `07-TEST-STRATEGY.md` §2's one minor unit, D050's "to the minor unit", and the 3.7b wording. The pattern is the finding — a tolerance is an arithmetic claim, and stating one without deriving it produces a test that passes by being kept small | Active |
