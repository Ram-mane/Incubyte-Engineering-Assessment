# The module packages arrive over Days 2 and 3. Until each exists, its rules match no classes,
# and an unmatched rule is not a failure - it is a rule waiting for the code it governs.
# Every rule here is proved against a deliberate violation before being committed.
archRule.failOnEmptyShould=false
